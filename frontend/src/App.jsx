import React from "react";
import { Routes, Route } from "react-router-dom";
import

export default function App() {
  return (
    <Routes>
      <Route path='' element={} />
      <Route path='' element={} />
      <Route path='' element={} />
      <Route path='' element={} />
      <Route path='' element={} />
    </Routes>
    );
}
